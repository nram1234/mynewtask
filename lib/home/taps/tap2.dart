import 'package:flutter/material.dart';

class Tap2 extends StatelessWidget {
  const Tap2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.cyan,);
  }
}
