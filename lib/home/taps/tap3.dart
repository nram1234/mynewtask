import 'package:flutter/material.dart';

class Tap3 extends StatelessWidget {
  const Tap3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amber,);
  }
}
